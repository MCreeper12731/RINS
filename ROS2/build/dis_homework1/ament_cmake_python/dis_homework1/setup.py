from setuptools import find_packages
from setuptools import setup

setup(
    name='dis_homework1',
    version='0.0.0',
    packages=find_packages(
        include=('dis_homework1', 'dis_homework1.*')),
)
